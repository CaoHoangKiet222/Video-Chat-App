import styled from "styled-components";
import {
  Avatar,
  ChatContent,
  ChatInfo,
  ChatItem,
  ChatText,
} from "./ChatItems.styled";

export const ChatGroupItem = styled(ChatItem)``;
export const ChatGroupAvatar = styled(Avatar)``;
export const ChatGroupContent = styled(ChatContent)``;
export const ChatGroupInfo = styled(ChatInfo)``;
export const ChatGroupText = styled(ChatText)``;
